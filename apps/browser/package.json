{
  "name": "@dbkang/browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @dbkang/userscript build && node scripts/build-extension.mjs",
    "build:windows": "node scripts/build-windows.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && node scripts/validate-windows-build.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "playwright-core": "1.56.1",
    "postject": "^1.0.0-alpha.6",
    "rcedit": "5.0.2",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
